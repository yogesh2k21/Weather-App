git clone https://github.com/yogesh2k21/Weather-App.git
apt install python3
apt install python3-pip
pip3 install virtualenv
virtualenv env
source env/bin/activate
pip install psycopg2-binary
apt-get install libpq-dev python-dev
pip install -r requirements.txt
python manage.py runserver 0:8000


for docker instance insise ec2
docker run -d -p 8001:8000 weatherapp

8001 is my host port and 8000 is container port, Now both are binded and we can access the container project in our host port 8001
